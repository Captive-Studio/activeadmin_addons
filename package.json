{
  "name": "@captive/activeadmin_addons",
  "version": "1.10.0",
  "description": "Set of addons to help with the activeadmin ui",
  "main": "src/all.js",
  "files": [
    "src/*"
  ],
  "repository": "https://github.com/platanus/activeadmin_addons.git",
  "author": "Platanus <rubygems@platan.us>",
  "license": "MIT",
  "scripts": {
    "build": "rollup --config rollup.config.js",
    "prepublishOnly": "rm -rf src && mkdir src && cp -R app/assets/stylesheets/activeadmin_addons src/stylesheets && cp -R app/javascript/activeadmin_addons/* src"
  },
  "dependencies": {
    "jquery-datetimepicker": "^2.5.21",
    "script-loader": "^0.7.2",
    "select2": "^4.0.13"
  }
}
